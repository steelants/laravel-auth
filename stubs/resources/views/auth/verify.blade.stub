<x-layout-auth>
<div>
    <h1>{{ __('auth.Verify') }}</h1>

    <div>
        @if (session('resent'))
            <div role="alert">
                {{ __('auth.sendEmail') }}
            </div>
        @endif

        {{ __('auth.checkEmail') }}
        {{ __('auth.notReceiveEmail') }},
        <form method="POST" action="{{ route('verification.resend') }}">
            @csrf
            <button type="submit">{{ __('auth.requestNew') }}</button>.
        </form>
    </div>
</div>
</x-layout-auth>
